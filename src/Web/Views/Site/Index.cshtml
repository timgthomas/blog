@model IEnumerable<Post>

@{ ViewBag.Title = "Index"; }

<ul>
@foreach (Post post in Model) {
	<li class=post>
		<header>
			<h1>@post.Title</h1>
			<h2>Posted on @post.Posted</h2>
		</header>
		@Html.Raw(post.Body)
	</li>
}
</ul>